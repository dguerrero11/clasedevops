# IMPORTANTE: Aplicar DESPUÉS de las reglas de allow para no tumbar la app
# Ver orden en clase5-live.md → sección "Orden de aplicación NetworkPolicy"
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: default
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
